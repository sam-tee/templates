# LaTeX template
- Run ``` direnv allow ``` if you are using direnv or ``` nix develop ``` if not
- For Overleaf-like setup run ``` watch-tex <filename> ```
- To compile to pdf run ``` compile-tex <filename> ```
- The full latexmk package is in the shell so you can use that for specific flags
